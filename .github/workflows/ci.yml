name: CI/CD StreamWave

on:
  push:
    branches: ["main"]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Cloner le dépôt
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Installer Docker
      - name: Set up Docker
        uses: docker/setup-buildx-action@v3

      # 3️⃣ Connexion à Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 4️⃣ Build et push de l’image backend
      - name: Build and push backend image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/streamwave-backend:latest ./backend
          docker push ${{ secrets.DOCKER_USERNAME }}/streamwave-backend:latest

      # 5️⃣ Build et push de l’image frontend
      - name: Build and push frontend image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/streamwave-frontend:latest ./frontend
          docker push ${{ secrets.DOCKER_USERNAME }}/streamwave-frontend:latest

      # 6️⃣ Fin du pipeline
      - name: ✅ Pipeline completed
        run: echo "🎉 StreamWave CI/CD déployé sur Docker Hub avec succès !"
# ✅ Docker Hub login test successful (Nada)

# retry docker login
# fix docker secrets
# retry docker login fix
